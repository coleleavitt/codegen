interface:
  display_name: "Codegen"
  short_description: "Repo-specific patterns and workflows for codegen"
  default_prompt: "Use the codegen repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true